# ControlToKey
Profiles are formatted as INI-style text files containing key-value mappings. Write a Python script to read the .cfg structure and convert button states (e.g., Button0=1) into mapped key presses.
